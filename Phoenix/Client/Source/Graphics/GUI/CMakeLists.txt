set(currentDir ${CMAKE_CURRENT_LIST_DIR})
set(Sources
	${Sources}

	${currentDir}/Container.cpp
	${currentDir}/BasicShapes.cpp
	${currentDir}/Button.cpp
	${currentDir}/ImSuperButton.cpp

	PARENT_SCOPE
)
